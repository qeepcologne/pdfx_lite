include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    # Ignore generated files
    - '**/*.g.dart'
    - '**/generated_plugin_registrant.dart'
    - '**/pigeon.dart'
    # Codegen input: imports package:pigeon, which is not a dev dependency (see pubspec.yaml)
    - 'pigeons/**'
